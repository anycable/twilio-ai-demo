<%= turbo_frame_tag :dialog do %>
  <dialog id="modal" class="p-0 rounded-2xl shadow-xl backdrop:bg-black/50" data-controller="dialog" data-dialog-open-value="true">
    <div class="p-8 w-[32rem] max-w-full">
    <h2 class="text-2xl font-bold mb-6 text-gray-800">Add Task</h2>
    <%= render "form", todo: Todo.new(deadline: Date.current) %>
  </dialog>
<% end %>
