<% content_for :title, "Phone calls monitoring" %>

<header class="glass-morphism rounded-xl p-6 mb-8 flex flex-col sm:flex-row justify-between items-center gap-4">
  <h1 class="text-4xl font-bold bg-gradient-to-r from-indigo-600 to-purple-600 text-transparent bg-clip-text">
    On your plate 🍽
  </h1>
  <nav class="flex space-x-2">
    <div class="flex flex-col items-start">
      <h2 class="text-2xl">Monitor phone calls</h2>
      <h3>Make a call at <strong><%= TwilioConfig.phone_number %></strong></h3>
    </div>
  </nav>
</header>

<div id="callList" class="space-y-6">
  <%= turbo_stream_from "phone_calls" %>
</div>
